version: '3.8'

services:
  # Baza danych dla Serwisu Auth (Użytkownicy) -> PORT 5431
  auth-db:
    image: postgres:15-alpine
    container_name: fitflow-auth-db
    environment:
      POSTGRES_DB: auth_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin_password
    ports:
      - "5431:5432"
    volumes:
      - auth_data:/var/lib/postgresql/data
    networks:
      - fitflow-network

  # Baza danych dla Serwisu Operations (Rezerwacje) -> PORT 5433
  operations-db:
    image: postgres:15-alpine
    container_name: fitflow-ops-db
    environment:
      POSTGRES_DB: operations_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin_password
    ports:
      - "5433:5432"
    volumes:
      - operations_data:/var/lib/postgresql/data
    networks:
      - fitflow-network

networks:
  fitflow-network:
    driver: bridge

volumes:
  auth_data:
  operations_data: